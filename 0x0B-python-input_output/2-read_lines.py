#!/usr/bin/python3
""" Read n lines """


def read_lines(filename="", nb_lines=0):
    """ reads n lines of a text file
    (UTF8) and prints it to stdout """
    with open(filename, 'w', encoding="utf8") as textfile:
        fde = textfile.write(text)
    return fde
